$.ajax({
	//.ajax is a function
	url: 'http://example.com',
	method: 'GET'
	//these are the arguments
}).done(function(response){
	//response is not a reserved keyword, i can define that
	//.done is making it so when the GET is done it will run the following function
})